# Multi-backend server: register communicators in schemas/backends.json (not LAB_VIEW_PATH).
# Optional override:
# CLOUDLABS_BACKENDS_CONFIG=schemas/backends.json
#
# Legacy single-backend (still supported):
# LAB_VIEW_PATH=backend/lab_communicator/mock/lab_view/
UVICORN_ACCESS_LOG=False
