# Single switch: point at a lab view bundle (communicator, lab_automation, layout, … live there).
LAB_VIEW_PATH=backend/lab_communicator/mock/lab_view
UVICORN_ACCESS_LOG=False
# MuJoCo geometry profile. Use optical_housings to render the exported CAD.
CLOUDLAB_SIM_PROFILE=demo_boxes
