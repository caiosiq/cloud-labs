/**
 * Landing page is a static marketing shell only.
 *
 * Lab communicators, lab-state polling, and session reconciliation start when
 * the user opens Twin (or another surface) and explicitly selects a backend —
 * not from `/`. This file is intentionally empty of Twin/lab imports so the
 * home route never touches `/api/lab-state` or related endpoints.
 */
