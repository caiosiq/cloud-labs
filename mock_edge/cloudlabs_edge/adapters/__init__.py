"""Filled Edge Contract adapters for the mock teaching host.

Same layout as ``cloudlabs-edge init``. Implementations call ``mock_edge.host``
through :mod:`adapters.context` (physics package stays outside this folder).
"""
