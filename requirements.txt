# Cloud Labs - Backend API (FastAPI)
# Install from project root: pip install -r requirements.txt
# Also install the author SDK: pip install -e ./packages/cloudlabs
# Run from backend/: uvicorn main:app --reload

# --- Cloud Labs (mock + API) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
python-dotenv>=1.0.0
mujoco>=3.3.0,<4.0

# --- Lab automation (REAL mode: when LAB_MODE=REAL and LAB_AUTOMATION_PATH is set) ---
# Core scientific stack
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0

# Vision & cameras
opencv-python>=4.8.0
pyrealsense2>=2.50.0

# Optimization (strategies: Cobyla, Newton, Bayesian)
scikit-learn>=1.2.0
scikit-optimize>=0.9.0

# Robot (xArm)
xArm-Python-SDK>=1.11.0

# Network & serial
requests>=2.28.0
pyserial>=3.5

# Imaging
Pillow>=9.0.0

# Optional: MindVision camera SDK (if using mvsdk in scripts)
# mvsdk  # uncomment if needed; may require vendor install
