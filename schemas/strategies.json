{
  "NEWTON": {
    "name": "Newton Strategy",
    "description": "Discrete table alignment: pixel-X centroid error, move on chosen table axis while hovering.",
    "loss_metrics": ["centroid_match"],
    "parameters": {
      "axis": {
        "type": "string",
        "enum": ["x", "y"],
        "default": "x",
        "description": "Table axis to nudge (camera loss is always pixel X)"
      },
      "tolerance_ratio": {
        "type": "float",
        "default": 0.05,
        "description": "Acceptable |pixel offset| as fraction of image width"
      },
      "video_exposure": {
        "type": "float",
        "default": 0.2,
        "description": "Recorder live-feed exposure (seconds)"
      }
    }
  },
  "COBYLA": {
    "name": "Cobyla Alignment",
    "description": "Motor knob alignment via reference subtraction; requires pinned lab optimization reference.",
    "loss_metrics": ["reference_match"],
    "parameters": {
      "video_exposure": {
        "type": "float",
        "default": 0.2,
        "description": "Recorder live-feed exposure (seconds)"
      }
    }
  }
}
